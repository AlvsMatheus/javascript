// função somar com os parametros a e b
function somar(a=0, b=0) {
    return a + b; // o objetivo/ordem e retornar a soma de a e b
}

// agora que a função já existe, podemos chama-la e transformar em uma variavel para exibir o resultado

console.log(somar(2, 4)) // exibir o resultado da soma de 2 + 4

let res = somar(2, 4) // ao colocar a action aqui eu já chamei ela
console.log(res) // exibir o resultado da soma de 2 + 4 */

//ambos os jeitos acima são validos, o primeiro é mais direto e o segundo é mais indireto